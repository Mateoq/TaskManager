/**
 * Module with the Div component used as a flexible component,
 * that can be styled using the prop css.
 * @module src/basic_components/Div
 */
import styled from 'styled-components';

const Div = styled.div``;

export default Div;
